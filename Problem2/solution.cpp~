#include <iostream>

using namespace std;

// gives the n-th fibonacci number
unsigned fib(unsigned index)
{
  if (index == 0)
    return 0;
  if (index == 1)
    return 1;

  return fib(index - 1) + fib(index - 2);
}

// parameters
// ...

int main()
{
  cout << "test" << endl;
  
  return 0;
}
